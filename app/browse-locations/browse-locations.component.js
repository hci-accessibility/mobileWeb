'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('browseLocations').
  component('browseLocations',
    {
      templateUrl: 'browse-locations/browse-locations.template.html',
      controller: function MapController(){}
    });
